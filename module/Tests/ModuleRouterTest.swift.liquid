//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest
@testable import {{ module_info.product_module_name }}

class {{ prefix }}{{ module_info.name }}ModuleRouterTest: XCTestCase {
    var router: {{ prefix }}{{ module_info.name }}ModuleRouter!
    var moduleInteractor: {{ prefix }}{{ module_info.name }}ModuleInteractorMock!

    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
        router = {{ prefix }}{{ module_info.name }}ModuleRouter()
        //moduleInteractor = {{ prefix }}{{ module_info.name }}ModuleInteractorMock()
        //router.moduleInteractor = moduleInteractor
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        router = nil
        //moduleInteractor = nil
        super.tearDown()
    }
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }
    
}

// sourcery: AutoMockable
//extension {{ prefix }}{{ module_info.name }}ModuleInteractor {}
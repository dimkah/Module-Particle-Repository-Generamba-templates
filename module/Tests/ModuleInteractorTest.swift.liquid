//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest
@testable import {{ module_info.product_module_name }}

class {{ prefix }}{{ module_info.name }}ModuleInteractorTests: XCTestCase {
    var interactor: {{ prefix }}{{ module_info.name }}ModuleInteractor!
    //var router: {{ prefix }}{{ module_info.name }}ModuleRouterTestProtocolMock!

    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
        interactor = {{ prefix }}{{ module_info.name }}ModuleInteractor()
        //router = {{ prefix }}{{ module_info.name }}ModuleRouterTestProtocolMock()
        //interactor.router = router
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        interactor = nil
        //router = nil
        super.tearDown()
    }
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }
    
}

// sourcery: AutoMockable
//extension {{ prefix }}{{ module_info.name }}ModuleRouterTestProtocol: {{ prefix }}{{ module_info.name }}ModuleInteractorOutputProtocol, Routable {}
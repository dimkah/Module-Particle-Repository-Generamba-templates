//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest
@testable import {{ module_info.product_module_name }}

class {{ prefix }}{{ module_info.name }}ParticlePresenterTest: XCTestCase {
    var presenter: {{ prefix }}{{ module_info.name }}ParticlePresenter!
    //var view: {{ prefix }}{{ module_info.name }}ParticleViewInputProtocolMock!
    //var interactor: {{ prefix }}{{ module_info.name }}ParticleInteractorInputProtocolMock!

    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
        presenter = {{ prefix }}{{ module_info.name }}ParticlePresenter()
        //view = {{ prefix }}{{ module_info.name }}ParticleViewInputProtocolMock()
        //interactor = {{ prefix }}{{ module_info.name }}ParticleInteractorInputProtocolMock()
        //presenter.view = view
        //presenter.interactor = interactor
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        presenter = nil
        //view = nil
        //interactor = nil
        super.tearDown()
    }
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }
    

}

// sourcery: AutoMockable
//extension {{ prefix }}{{ module_info.name }}ParticleViewInputProtocol {}

// sourcery: AutoMockable
//extension {{ prefix }}{{ module_info.name }}ParticleInteractorInputProtocol {}